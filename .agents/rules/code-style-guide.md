---
trigger: always_on
---

- Make sure to have separate scss module files for each react component
- Always use Typescript while building React components
- Use descriptive names for variables, functions etc.
- Avoid using comments unless the process is complex
- Make sure to add passwords and tokens in .env file
- make sure .env file is never committed to git repository
- make separate custom hooks as much as possible.
